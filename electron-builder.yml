appId: com.markdown-viewer.app
productName: Markdown Viewer

directories:
  output: release

files:
  - out/**/*
  - package.json

win:
  target: nsis
  artifactName: 'Markdown-Viewer-Setup-${version}.${ext}'

mac:
  target: dmg

linux:
  target: 
    - target: AppImage
    - target: deb
  maintainer: "Markdown Viewer Team <team@markdown-viewer.local>"
